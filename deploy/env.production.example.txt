# ── MyAccentTrainer — production environment variables ────────────────
# These go into Vercel's "Environment Variables" settings, NOT in a file
# you commit. Vercel fills DATABASE_URL in automatically when you connect
# a Prisma Postgres database — you only need to add the rest.

# Database — set automatically by the Vercel + Prisma Postgres integration.
DATABASE_URL="(added for you by Vercel when you connect the database)"

# Auth — REQUIRED in production. Use a long random string (40+ characters).
# Generate one at https://generate-secret.vercel.app/40
JWT_SECRET="paste-a-long-random-string-here"

# The public web address of your live site (fill in after first deploy).
NEXT_PUBLIC_APP_URL="https://your-project.vercel.app"

# Pronunciation scoring (optional — app works without it).
AZURE_SPEECH_KEY=""
AZURE_SPEECH_REGION=""

# Payments (optional — add when ready to charge).
STRIPE_SECRET_KEY=""
STRIPE_PRICE_ID=""
STRIPE_WEBHOOK_SECRET=""
